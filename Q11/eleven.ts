// Date: 2024-06-01
// This program stores the names of a few friends in an array and prints each name.

const names: string[] = ["Alice", "Bob", "Charlie", "David", "Eve"]; // Array storing friends' names

// Loop through the array and print each name
names.forEach(name => {
    console.log(name); // Printing each friend's name
});